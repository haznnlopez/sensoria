/* ============================================================
   sensoria — ethics.js
   Page-specific JavaScript for ethics.html
   Shared scripts (nav close, fade-up, sidebar) live in shared.js
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // Add ethics-specific JS below
});
