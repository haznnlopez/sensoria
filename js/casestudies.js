/* ============================================================
   sensoria — casestudies.js
   Page-specific JavaScript for casestudies.html
   Shared scripts (nav close, fade-up, sidebar) live in shared.js
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // Add casestudies-specific JS below
});
