/* ============================================================
   sensoria — feedback.js
   Page-specific JavaScript for feedback.html
   Shared scripts (nav close, fade-up, sidebar) live in shared.js
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // Add feedback-specific JS below
});
