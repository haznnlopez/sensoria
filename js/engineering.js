/* ============================================================
   sensoria — engineering.js
   Page-specific JavaScript for engineering.html
   Shared scripts (nav close, fade-up, sidebar) live in shared.js
   ============================================================ */

document.addEventListener('DOMContentLoaded', () => {
  // Add engineering-specific JS below
});
